
# Default values for paths-example.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: quay.io/jcarvaja/quarkus-apps-catalog-enricher
  tag: 1.0.0-SNAPSHOT
  pullPolicy: IfNotPresent
service:
  name: catalog-rest-api
  type: ClusterIP
  externalPort: 8083
  internalPort: 8083
database:
  url: jdbc:postgresql://quarkusappcatalog.postgresql-qe-infra:5432/quarkusappcatalog
  user: root
  pwd: EWQNNZC
  schema: quarkusappcatalog
kafka:
  bootstrap: kafka-qe-kafka-bootstrap.kafka-qe-infra:9092